import { Dillers } from "./dillers";

export class Filial {
  id?: string;
  phoneNumber?: string;
  address?: string;
  accountNumber?: string;
  serialNumber?: string;
  bankNumber?: string;
  cardNumber?: string;
  typeFilial?: string;
  tranzitAccount?:string;
  inn?:string
  employer?: string;
  diller?: Dillers;
}
